<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raproject.whattowatch">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".di.DiApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Whattowatch">
        <activity
            android:name=".ui.about_content.AboutContentActivity"
            android:exported="false"
            android:label="@string/title_activity_about_content"
            android:theme="@style/Theme.Whattowatch" />
        <activity
            android:name=".ui.content_list.AppActivity"
            android:exported="true"
            android:label=""
            android:theme="@style/Theme.Whattowatch.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>